# 🚀 Писател (Book Writer) - Пълно Ръководство за Настройка

Това ръководство описва подробно как да настроите всички компоненти на приложението, включително Gemini AI, Google OAuth за Drive интеграция, и как работи всичко.

---

## 📋 Съдържание

1. [Изисквания](#изисквания)
2. [Инсталация](#инсталация)
3. [Gemini API Настройка](#gemini-api-настройка)
4. [Google OAuth Настройка (за Google Drive)](#google-oauth-настройка)
5. [Environment Variables](#environment-variables)
6. [Стартиране на Приложението](#стартиране-на-приложението)
7. [Как Работи Архитектурата](#как-работи-архитектурата)
8. [Отстраняване на Проблеми](#отстраняване-на-проблеми)

---

## Изисквания

- **Node.js** 18.x или по-нова версия
- **npm** или **pnpm**
- **Google акаунт** (за Gemini API и Google Drive)
- Модерен уеб браузър

---

## Инсталация

### 1. Клониране/Отваряне на проекта

```bash
cd c:\Coding\book-writer
```

### 2. Инсталиране на зависимостите

```bash
npm install
```

### 3. Копиране на environment файла

```bash
copy .env.example .env.local
```

---

## Gemini API Настройка

Gemini е AI моделът, който генерира съдържание за книгите.

### Стъпка 1: Получаване на API ключ

1. Отидете на **[Google AI Studio](https://aistudio.google.com/apikey)**
2. Влезте с вашия Google акаунт
3. Кликнете на **"Create API Key"**
4. Копирайте генерирания ключ

### Стъпка 2: Добавяне на ключа

Отворете `.env.local` и добавете:

```env
GEMINI_API_KEY=вашият_api_ключ_тук
```

### Как работи Gemini интеграцията

Приложението използва два модела:

| Модел | Употреба | Характеристики |
|-------|----------|----------------|
| `gemini-3-pro-preview` | Генериране на съдържание, планове | По-умен, по-бавен |
| `gemini-3-flash-preview` | Граматика, съкращаване | По-бърз, по-евтин |

**Файлове:**
- `src/lib/ai/gemini-client.ts` - Основен клиент за комуникация с Gemini
- `src/lib/ai/prompts/system-prompts.ts` - Системни промптове за различните агенти
- `src/app/api/ai/*` - API routes за AI операции

---

## Google OAuth Настройка

Google OAuth е необходим за запазване на проекти в Google Drive.

### Стъпка 1: Създаване на Google Cloud Project

1. Отидете на **[Google Cloud Console](https://console.cloud.google.com/)**
2. Кликнете на падащото меню за проекти (горе вляво)
3. Кликнете **"New Project"**
4. Име: `book-writer` (или каквото искате)
5. Кликнете **"Create"**

### Стъпка 2: Активиране на APIs

1. Отидете на **APIs & Services > Library**
2. Търсете и активирайте:
   - **Google Drive API** - за файлове
   - **Google Docs API** - за документи

### Стъпка 3: Конфигуриране на OAuth Consent Screen

1. Отидете на **APIs & Services > OAuth consent screen**
2. Изберете **"External"** (или Internal ако сте в организация)
3. Попълнете:
   - **App name**: `Писател` или `Book Writer`
   - **User support email**: вашият имейл
   - **Developer contact email**: вашият имейл
4. Кликнете **"Save and Continue"**

### Стъпка 4: Добавяне на Scopes

1. На страницата за scopes, кликнете **"Add or Remove Scopes"**
2. Добавете следните scopes:
   ```
   openid
   email
   profile
   https://www.googleapis.com/auth/drive.file
   https://www.googleapis.com/auth/documents
   ```
3. Кликнете **"Save and Continue"**

### Стъпка 5: Добавяне на Test Users (за External)

1. На страницата за Test users
2. Кликнете **"Add Users"**
3. Добавете имейла на баща ви и вашия имейл
4. Кликнете **"Save and Continue"**

### Стъпка 6: Създаване на OAuth Credentials

1. Отидете на **APIs & Services > Credentials**
2. Кликнете **"Create Credentials" > "OAuth client ID"**
3. Application type: **"Web application"**
4. Name: `Book Writer Web Client`
5. **Authorized JavaScript origins:**
   ```
   http://localhost:3000
   ```
6. **Authorized redirect URIs:**
   ```
   http://localhost:3000/api/auth/callback/google
   ```
7. Кликнете **"Create"**
8. **ВАЖНО:** Копирайте `Client ID` и `Client Secret`

### Стъпка 7: Добавяне на Credentials в .env.local

```env
GOOGLE_CLIENT_ID=вашият_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=вашият_client_secret
```

### Стъпка 8: Генериране на NextAuth Secret

Отворете терминал и изпълнете:

```bash
# На Windows PowerShell:
[Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }) -as [byte[]])

# Или просто използвайте произволен дълъг низ (поне 32 символа)
```

Добавете в `.env.local`:

```env
NEXTAUTH_SECRET=вашият_генериран_secret_тук
NEXTAUTH_URL=http://localhost:3000
```

---

## Environment Variables

### Пълен .env.local файл

```env
# ===== GEMINI AI =====
# Получете от: https://aistudio.google.com/apikey
GEMINI_API_KEY=AIza...вашият_ключ

# ===== GOOGLE OAUTH =====
# Получете от: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=123456789-abc.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-...

# ===== NEXTAUTH =====
# Генерирайте secret с командата по-горе
NEXTAUTH_SECRET=вашият_дълъг_random_string_минимум_32_символа
NEXTAUTH_URL=http://localhost:3000

# ===== APP =====
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

---

## Стартиране на Приложението

### Development Mode

```bash
npm run dev
```

Отворете http://localhost:3000 в браузъра.

### Production Build

```bash
npm run build
npm start
```

---

## Как Работи Архитектурата

### Обща Схема

```
┌─────────────────────────────────────────────────────────────┐
│                        КЛИЕНТ (Browser)                      │
├─────────────────────────────────────────────────────────────┤
│  React Components                                            │
│  ├── TipTap Editor (богат текстов редактор)                 │
│  ├── Chat Panel (с @ mentions)                              │
│  ├── Sidebar (навигация)                                    │
│  └── Pages (plan, write, characters, locations, settings)   │
├─────────────────────────────────────────────────────────────┤
│  Zustand Stores (state management)                          │
│  ├── project-store.ts (проекти, глави, MasterJSON)         │
│  ├── chat-store.ts (чат съобщения, контекст)               │
│  └── ui-store.ts (UI състояние)                            │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     СЪРВЪР (Next.js API)                    │
├─────────────────────────────────────────────────────────────┤
│  API Routes                                                  │
│  ├── /api/ai/generate     - Генериране на съдържание        │
│  ├── /api/ai/edit         - Редактиране (съкрати, разшири)  │
│  ├── /api/ai/plan         - Генериране на план              │
│  ├── /api/ai/analyze-style- Анализ на стил                  │
│  ├── /api/ai/chat         - Чат с AI                        │
│  ├── /api/auth/[...nextauth] - Google OAuth                 │
│  └── /api/drive/*         - Google Drive операции           │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    ВЪНШНИ УСЛУГИ                            │
├─────────────────────────────────────────────────────────────┤
│  Google Gemini API                                          │
│  ├── gemini-3-pro-preview (умен, за писане)                │
│  └── gemini-3-flash-preview (бърз, за редакции)            │
├─────────────────────────────────────────────────────────────┤
│  Google APIs                                                │
│  ├── Drive API (запазване на файлове)                      │
│  └── Docs API (създаване на Google Docs)                   │
└─────────────────────────────────────────────────────────────┘
```

### Поток на Данните

#### 1. Създаване на Проект
```
User → New Project Form → project-store.createProject() → localStorage
```

#### 2. Генериране на План
```
User → "Генерирай план" бутон
  → /api/ai/plan (POST)
  → Gemini API (с PLAN_GENERATION_AGENT_PROMPT)
  → JSON план
  → project-store.updatePlan()
  → localStorage
```

#### 3. Писане на Глава
```
User → TipTap Editor → "Генерирай глава" бутон
  → /api/ai/generate (POST)
    → Изпраща: chapter info, MasterJSON, previous chapters
  → Gemini API (с WRITING_AGENT_PROMPT)
  → Генериран текст
  → TipTap Editor (insertContent)
```

#### 4. AI Редакция (Съкрати/Разшири/Пренапиши)
```
User → Селектира текст → AI бутон в toolbar
  → /api/ai/edit (POST)
    → action: "shorten" | "expand" | "rewrite" | "grammar"
    → text: избраният текст
  → Gemini API (с EDITOR_AGENT_PROMPT)
  → Редактиран текст
  → TipTap Editor (replaceSelection)
```

#### 5. Запазване в Google Drive
```
User → "Запази в Drive" бутон
  → Проверка за сесия (NextAuth)
  → Ако няма сесия → Google OAuth login
  → /api/drive/save (POST)
    → Google Drive API (създава/обновява файл)
    → Google Docs API (форматиране)
  → Връща fileId
```

### MasterJSON Система

MasterJSON е "библията" на проекта - съдържа всичко за историята:

```typescript
{
  projectMetadata: { title, genre, style, wordCounts },
  characters: {
    permanent: { [id]: Character },  // Постоянни характеристики
    timeline: []                      // Промени през времето
  },
  locations: {
    permanent: { [id]: Location },
    timeline: []
  },
  plotElements: { mainPlot, subplots, themes, motifs },
  worldBuilding: { rules, history, culture },
  continuityRules: [],                // Правила които не трябва да се нарушават
  styleGuide: { tone, pov, tense, vocabulary, avoidWords },
  timeline: { events, currentPosition }
}
```

### AI Агенти

| Агент | Файл | Роля |
|-------|------|------|
| MasterJSON Agent | `system-prompts.ts` | Пазител на консистентността |
| Style Analysis Agent | `system-prompts.ts` | Анализира и копира стил |
| Plan Generation Agent | `system-prompts.ts` | Създава структура на книгата |
| Writing Agent | `system-prompts.ts` | Пише проза |
| Editor Agent | `system-prompts.ts` | Редактира текст |
| Grammar Agent | `system-prompts.ts` | Поправя граматика |

---

## Отстраняване на Проблеми

### "GEMINI_API_KEY is not configured"
- Проверете дали `.env.local` съществува
- Проверете дали ключът е правилен (започва с `AIza`)
- Рестартирайте dev сървъра след промени в .env

### Google OAuth грешки

#### "redirect_uri_mismatch"
- Проверете дали Redirect URI в Google Console е точно:
  ```
  http://localhost:3000/api/auth/callback/google
  ```
- Внимавайте за trailing slashes!

#### "Access blocked: app not verified"
- Добавете потребителя като Test User в OAuth consent screen
- Или публикувайте приложението (изисква верификация)

#### "invalid_client"
- Client ID или Client Secret са грешни
- Копирайте ги отново от Google Console

### TipTap Editor не зарежда
- Проверете конзолата за грешки
- Уверете се, че всички TipTap пакети са инсталирани:
  ```bash
  npm install @tiptap/react @tiptap/pm @tiptap/starter-kit @tiptap/extension-placeholder
  ```

### Локалното съхранение не работи
- Проверете дали браузърът не е в Private/Incognito режим
- Изчистете localStorage и опитайте отново:
  ```javascript
  localStorage.clear()
  ```

---

## Deployment на Vercel

### 1. Push към GitHub

```bash
git add .
git commit -m "Ready for deployment"
git push origin main
```

### 2. Свържете с Vercel

1. Отидете на [vercel.com](https://vercel.com)
2. Import вашия GitHub repo
3. Добавете Environment Variables (същите като .env.local)
4. Deploy!

### 3. Обновете Google OAuth

След deploy, добавете production URL към Google Console:

**Authorized JavaScript origins:**
```
https://вашето-приложение.vercel.app
```

**Authorized redirect URIs:**
```
https://вашето-приложение.vercel.app/api/auth/callback/google
```

И обновете `NEXTAUTH_URL` в Vercel environment variables.

---

## Бързи Команди

```bash
# Стартиране
npm run dev

# Build
npm run build

# Lint
npm run lint

# Инсталиране на нови пакети
npm install package-name
```

---

*Последна актуализация: 5 Януари 2026*
